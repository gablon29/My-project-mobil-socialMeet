import React from "react";
import { View, Text } from "react-native";

export const Reset3Step = ({ password, setPassword }) => {
  return (
    <View>
      <Text>acá cambuia la contraseña</Text>
    </View>
  );
};
