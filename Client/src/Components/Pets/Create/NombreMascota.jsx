import React from "react";
import { View, Text } from "react-native";

const NombreMascota = ({ Pantalla }) => {
  return (
    <View>
        <Text>{Pantalla}</Text>
    </View>
    )
};

export default NombreMascota;
